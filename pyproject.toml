[project]
name = "gemini-model-updates-feed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "google-cloud-storage>=3.10.1",
    "ruff>=0.15.15",
]
